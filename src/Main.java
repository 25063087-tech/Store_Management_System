// Main entry point of the Store Operations Management System
// Initializes core managers and starts the application

public class Main {

    // Program entry point
    //Loads system data and initializes the login process

    public static void main(String[] args) {
        FileManager fm = new FileManager();
        EmployeeManager em = new EmployeeManager();
    }
}
